using UnityEngine;
using System.Collections;

public class ArrowPointer : MonoBehaviour {

	public Transform schoolPosition;

	// Use this for initialization
	void Start () {
		// We want to point our arrow in the position of the school 
		// We also want to keep it in the ref frame of our camera... hmmmmm
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
